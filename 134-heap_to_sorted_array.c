#include "binary_trees.h"

/**
 * heap_to_sorted_array - convert a Binary Max Heap.
 *
 * @heap: ptr to the root node of the heap.
 *
 * @size: ptr to store the size of the arr.
 *
 * Return: the generated array or NULL upon failure.
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{

}

#include "binary_trees.h"

/**
 * heap_extract - func will extract the root node of a Max Binary Heap.
 *
 * @root: double ptr to the root node of heap.
 *
 * Return: the val stored in the root node or 0 upon failure.
 */
int heap_extract(heap_t **root)
{

}
